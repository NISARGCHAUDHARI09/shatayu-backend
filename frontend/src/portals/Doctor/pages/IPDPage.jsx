import IPD from '../../../components/modules/IPD/IPD';

// Use the shared IPD component for the doctor portal pages to keep behavior
// consistent with the admin portal and avoid duplicate maintenance.
export default IPD;
