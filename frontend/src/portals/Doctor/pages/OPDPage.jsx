import OPD from '../../../components/modules/OPD/OPD';

// Re-export the shared OPD module into the doctor portal so the sidebar links
// open the prebuilt OPD component instead of maintaining a duplicate page.
export default OPD;
